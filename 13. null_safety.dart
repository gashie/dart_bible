void main() {
// every declared variabled should be initialised, if not, specify it as null
// to declare string, put question mark infront of the data type,

  int? x;
  print(x);

  print(x?.isEven);
}
