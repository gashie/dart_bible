void main() {
  int x = 25;
  int y = 45;

  final sum = x + y; //i declared as final because this value will never change
  print(sum);
  final result =
      x - y; //i declared as final because this value will never change
  print(result);
}
